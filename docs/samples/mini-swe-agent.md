# mini-swe-agent：用最小循环看清执行边界

## 为什么值得单独看

mini-swe-agent 很适合作为「最小可读 Agent Harness」样本，因为它把模型、Agent 与 Environment 三个角色压缩在很短的源码路径里，读者可以沿着一次模型查询、动作执行、观察回填和退出信号完成闭环核对。本专题关心这种最小结构暴露了哪些边界，不会因为代码量少就说它能力弱，也不会因为结构清楚就说它默认安全或足以用于生产。

它与六条一级主线是补充关系，不能相互替代。一级主线负责系统讲清配置、会话、权限、扩展和产品表面，这个样本则像一个可以拆开的教学模型，让读者在较小的搜索空间里验证 Agent Loop 的必要组成。当重试、限额、轨迹保存和执行环境都明确出现在源码中时，读者也能顺势分清哪些事实可以直接核对，哪些仍然需要外部实验。

![mini-swe-agent 最小循环与执行边界](../../assets/diagrams/samples/mini-swe-agent-loop.svg)

## 最小循环保留了哪些必要状态

`DefaultAgent.run` 会先清空消息，加入系统消息和用户消息，然后开始重复调用 `step`。每次 `step` 只完成一组连续动作，它先查询模型，再把返回消息里的动作交给环境执行，执行输出随后由模型适配器格式化成观察消息，重新放回消息历史。这样一来，「模型决定下一步」与「环境产生事实」被明确分开，也就形成了最小的查询、执行与观察循环。

终止不只发生在成功提交时——每次查询之前都会检查步数、累计成本和墙钟时间，连续格式错误也有自己的退出状态，未捕获异常则会在保存轨迹后继续向外抛出。循环只有在最后一条消息的角色为 `exit` 时才结束，并返回该消息的 `extra`，因此 `Submitted`、`LimitsExceeded`、`TimeExceeded` 和错误退出描述的只是控制流结果，不能直接映射成业务任务的通过或失败。

另一个关键机制是环境可以替换，而每种环境都代表不同的风险边界。`LocalEnvironment` 的类说明明确写着直接在本机执行 Bash 命令，实际调用也会把进程环境与配置环境合并后交给运行函数，所以它提供的是一条简单、透明的执行路径，并不会自动附带容器、虚拟机或系统调用过滤。

本地环境会用输出第一行的特殊提交标记触发 `Submitted`，并把后续文本当作 submission。这个协议很适合把「Agent 主动结束」从普通命令输出里分离出来，但它终究只是 Harness 内部的终止约定——恶意命令或错误命令同样能够打印这段字符串。责任必须分开。外部产物检查、测试和评分器仍要独立工作。

## 从哪里开始读源码

可以先读 [`DefaultAgent` 的最小循环](https://github.com/SWE-agent/mini-swe-agent/blob/25941c89cfbc91eb40b3f8756348c91d9977d57e/src/minisweagent/agents/default.py#L88-L157)，其中 `run` 定义循环，`query` 定义资源门限和模型调用，`execute_actions` 则定义环境执行与观察回填。接着读 [`LocalEnvironment.execute`](https://github.com/SWE-agent/mini-swe-agent/blob/25941c89cfbc91eb40b3f8756348c91d9977d57e/src/minisweagent/environments/local.py#L19-L55)，确认本地执行怎样选择工作目录、继承环境、捕获异常以及识别提交标记。

如果需要进一步验证恢复行为，再看 DefaultAgent 怎样分别处理 `FormatError`、`InterruptAgentFlow` 与未捕获异常。阅读时可以把每个异常分支整理成五列表格，逐项记录是否记账、是否追加消息、是否继续循环、是否形成退出状态，以及是否保存轨迹，以免把所有重试都笼统叫作恢复。

## 把模型动作、环境观察和退出协议连起来

模型与 Agent 之间靠一条完整消息衔接，而不是靠流式半成品。`query` 会在调用前检查步数、成本和时间，返回后再把响应与费用写入历史，只有已经完成的动作才会交给 `execute_actions`。教学实现最好让模型适配器负责解析动作，让 Agent 只编排查询与执行，这样就能用录制响应独立验证循环，不必依赖活模型的随机性。

Agent 与 Environment 之间传递的是动作输入和观察输出，Environment 接收工作目录、命令与环境变量，再返回退出状态、输出或控制异常，模型适配器随后把观察格式化成下一轮消息。调用 ID、动作原文、执行目录和返回码必须同时保存，否则 LocalEnvironment 产生的宿主错误很容易被记成模型错误。

退出协议与外部 Eval 构成第三个接缝，Submitted、限额和错误状态会进入 Trace，但 submission 只是候选产物，真正的结果还要由 Scorer 读取文件与测试来判断。最小原型可以让命令打印提交标记，却故意写错文件，用这个结果验证 Agent 即使正常退出，任务仍可能失败，从机制上阻断「框架结束等于任务通过」的推断。

## 沿一次最小任务走完整链

1. 入口把任务渲染进系统模板和实例模板，形成初始消息历史。
2. 查询前检查调用次数、累计成本与运行时长，超过门限就追加退出消息。
3. 模型根据当前消息返回文本与零个或多个动作，Harness 记录调用成本。
4. Environment 逐个执行动作，模型适配器把结果转成观察消息，再进入下一轮。
5. 格式错误可以在上限内恢复；退出消息、提交标记或硬错误使当前运行停止。
6. 外部验证读取轨迹和文件产物，以冻结任务契约判断结果。

这条链应当优先保留模型调用序号、累计成本、动作原文、工作目录、命令返回码、异常类型、退出状态和 submission 等观测字段。如果只保存最终回答，工具失败与恢复过程就会丢失，而只保存完整日志却不固定任务、配置和来源 Commit，同样无法复现当时的运行条件。

## 它补充了六条主课程的什么

对 DeepSeek Harness、Codex、Gemini CLI、Claude、pi 和 OpenCode 的主线分析，需要覆盖产品与工程的完整面貌，而 mini-swe-agent 不参与谁更强的横向排序。它提供的是一个基准骨架，用来追问任何更复杂 Harness 的模型请求在哪里形成、工具动作在哪里解释、真实执行在哪里发生、观察怎样回到上下文，以及退出最终由谁判定。

它也能帮助评测设计保持分层，因为 Harness 的退出状态只进入运行轨迹，submission 只是候选产物，Evaluator 还要继续读取冻结任务和最终文件。这些职责没有消失，它们只是没有全部内置。

## 什么时候值得采用

mini-swe-agent 适合用来学习循环、动作解释、观察回填、资源限额和环境替换，也适合构造确定性的状态机实验，但它不能单独证明企业权限治理、多客户端协议、长期会话、分布式恢复或发布评测。核心实现写得简短，并不会让这些责任自动出现。

LocalEnvironment 适合可信临时目录里的教学和调试，面对不可信任务、未知仓库或敏感凭据时，则应选择明确隔离的环境，并验证挂载、网络和进程权限是否真实生效。本文只覆盖锁定的 DefaultAgent 与 LocalEnvironment 路径，不评价其他环境或部署。它的迁移价值在于边界清楚，并不是主张实现越小越好。

## 最容易误判的地方

第一类风险是把 LocalEnvironment 当成沙箱，因为它会直接继承宿主进程能够访问的文件、命令和网络，即使调用前存在模型提示词或命令格式约束，也无法替代操作系统隔离。需要隔离时，应明确选择 Docker 等不同的环境实现，或者在 Harness 外提供受限账号、容器和网络策略，并验证这些限制是否真的生效。

第二类风险是把框架退出当成产品成功，因为打印提交标记、触发 `exit` 或返回 submission，都不能证明测试已经通过、需求已经满足，或者运行没有造成破坏性副作用。第三类风险是通过重试把格式错误包装成通过，基础设施恢复后可以再次尝试，但真实任务失败不能靠反复运行并只保留一次成功来改写。

## 怎样亲手核对

可以建立三个最小实验。正常链让模型返回一个只读动作，并确认观察消息进入下一轮。限额链把 step limit 设为一，检查第二次查询前是否形成 `LimitsExceeded`。边界链则在临时目录运行 LocalEnvironment，记录它确实使用了宿主进程与环境。所有实验都只证明锁定版本在受控输入下的行为，不能外推到其他环境、模型或部署。

核对时要同时检查两组信号，其中控制流信号包括退出状态、调用次数与异常，任务信号则包括预期文件差异、测试、禁止副作用和评分结果。只有任务信号满足冻结任务契约时，结果才算通过。控制流日志只能解释结果，不能替代结果本身。

## 读完后做四个判断

### 问题 1

最小实现是否意味着没有恢复？

**答案：** 不是。源码包含格式错误恢复、限额和轨迹保存，只是恢复策略较集中，必须逐分支核对。

### 问题 2

LocalEnvironment 是否默认安全？

**答案：** 不能这样判断。它明确直接执行宿主命令，安全边界取决于外部进程权限和所选环境。

### 问题 3

出现 Submitted 是否可以计为通过？

**答案：** 不可以。它只表示 Harness 收到提交协议，仍需独立检查产物和任务契约。

### 问题 4

为什么它只是扩展样本？

**答案：** 它突出最小循环的教学价值，但不承担一级主线需要的完整产品表面、部署与治理覆盖。
