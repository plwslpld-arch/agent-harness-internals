# mini-swe-agent：用最小循环看清执行边界

## 为什么值得单独看

mini-swe-agent 适合作为「最小可读 Agent Harness」样本。它把模型、Agent 与 Environment 三个角色压缩到很短的源码路径，使读者能沿着一次模型查询、动作执行、观察回填和退出信号完成闭环核对。本专题关心的是这种最小结构揭示了什么边界，不把代码量少解释成能力弱，也不把结构清楚解释成默认安全或生产完整。

它与六条一级主线的关系是补充而非替代。一级主线负责系统讲清配置、会话、权限、扩展和产品表面；这个样本则像一个可拆开的教学模型，让读者在较小搜索空间内验证 Agent Loop 的必要组成，并观察当重试、限额、轨迹保存和执行环境都显式出现时，哪些事实可以从源码得到，哪些仍然需要外部实验。

![mini-swe-agent 最小循环与执行边界](../../assets/diagrams/samples/mini-swe-agent-loop.svg)

## 最小循环保留了哪些必要状态

`DefaultAgent.run` 先清空消息并加入系统消息与用户消息，然后重复调用 `step`。`step` 本身只做一件组合动作：查询模型，再把返回消息中的动作交给环境执行。执行输出由模型适配器格式化为观察消息，重新进入消息历史。这个结构把「模型决定下一步」与「环境产生事实」分开，形成最小的查询、执行、观察循环。

终止并非只有成功提交。每次查询之前会检查步数、累计成本和墙钟时间；连续格式错误也有自己的退出状态；未捕获异常在保存轨迹后继续抛出。循环只在最后一条消息角色为 `exit` 时结束，并返回该消息的 `extra`。因此 `Submitted`、`LimitsExceeded`、`TimeExceeded` 和错误退出描述的是控制流结果，不能直接映射为业务任务的通过或失败。

另一个关键机制是环境可以替换，但每个环境代表不同风险边界。`LocalEnvironment` 的类说明明确写着直接在本机执行 Bash 命令，实际调用也把进程环境与配置环境合并后交给运行函数。它提供了简单、透明的执行路径，却没有自动附带容器、虚拟机或系统调用过滤。

本地环境用输出第一行的特殊提交标记触发 `Submitted`，并把后续文本当作 submission。这个协议很适合把「Agent 主动结束」从普通命令输出中分离出来，但它仍然只是 Harness 内的终止约定。恶意或错误命令同样可以打印该字符串，所以提交标记必须与外部产物检查、测试和评分器分责。

## 从哪里开始读源码

建议先读 [`DefaultAgent` 的最小循环](https://github.com/SWE-agent/mini-swe-agent/blob/25941c89cfbc91eb40b3f8756348c91d9977d57e/src/minisweagent/agents/default.py#L88-L157)：`run` 定义循环，`query` 定义资源门限和模型调用，`execute_actions` 定义环境执行与观察回填。之后读 [`LocalEnvironment.execute`](https://github.com/SWE-agent/mini-swe-agent/blob/25941c89cfbc91eb40b3f8756348c91d9977d57e/src/minisweagent/environments/local.py#L19-L55)，确认本地执行怎样选择工作目录、继承环境、捕获异常和识别提交标记。

需要进一步验证恢复行为时，再看 DefaultAgent 对 `FormatError`、`InterruptAgentFlow` 与未捕获异常的不同处理。这里的一个实用阅读方法是把每个异常分支写成「是否记账、是否追加消息、是否继续循环、是否形成退出状态、是否保存轨迹」五列表格，避免把所有重试都笼统叫作恢复。

## 把模型动作、环境观察和退出协议连起来

模型与 Agent 的接缝是一条完整消息，而不是流式半成品。`query` 在调用前检查步数、成本和时间，返回后把响应与费用写入历史；只有完成的动作才交给 `execute_actions`。教学实现应让模型适配器负责解析动作，让 Agent 只编排查询与执行，从而可以用录制响应独立验证循环，不依赖活模型随机性。

Agent 与 Environment 的接缝是动作输入和观察输出。Environment 接收工作目录、命令与环境变量，返回退出状态、输出或控制异常；模型适配器再把观察格式化为下一轮消息。调用 ID、动作原文、执行目录和返回码需要同时保存，否则 LocalEnvironment 的宿主错误会被误写成模型错误。

退出协议与外部 Eval 是第三个接缝。Submitted、限额和错误状态进入 Trace，submission 只是候选产物；Scorer 读取真实文件和测试判断结果。最小原型可以让命令打印提交标记但故意写错文件，验证 Agent 正常退出而任务仍失败，从机制上阻断「框架结束等于任务通过」。

## 沿一次最小任务走完整链

1. 入口把任务渲染进系统模板和实例模板，形成初始消息历史。
2. 查询前检查调用次数、累计成本与运行时长，超过门限就追加退出消息。
3. 模型根据当前消息返回文本与零个或多个动作，Harness 记录调用成本。
4. Environment 逐个执行动作，模型适配器把结果转成观察消息，再进入下一轮。
5. 格式错误可以在上限内恢复；退出消息、提交标记或硬错误使当前运行停止。
6. 外部验证读取轨迹和文件产物，以冻结任务契约判断结果。

这条链最值得保留的观测字段包括模型调用序号、累计成本、动作原文、工作目录、命令返回码、异常类型、退出状态和 submission。只保存最终回答会失去工具失败与恢复过程；只保存完整日志而不固定任务、配置和来源 Commit，又无法复现同一运行条件。

## 它补充了六条主课程的什么

对 DeepSeek Harness、Codex、Gemini CLI、Claude、pi 和 OpenCode 的主线分析要覆盖产品与工程全貌；mini-swe-agent 不参与谁更强的横向排序。它提供的是一个基准骨架：任何更复杂 Harness 都可以被追问模型请求在哪里形成、工具动作在哪里解释、真实执行在哪里发生、观察怎样回到上下文、退出由谁判定。

它也能帮助评测设计保持分层。Harness 的退出状态进入运行轨迹，submission 只是候选产物，Evaluator 再读取冻结任务和最终文件。最小循环没有省掉这些职责，只是没有把它们全部内置。

## 什么时候值得采用

mini-swe-agent 适合学习循环、动作解释、观察回填、资源限额和环境替换，也适合构造确定性状态机实验。它不适合单独证明企业权限治理、多客户端协议、长期会话、分布式恢复或发布评测，因为这些责任并未因核心简短而自动存在。

LocalEnvironment 适合可信临时目录中的教学和调试；面对不可信任务、未知仓库或敏感凭据时，应选择明确隔离的环境并验证挂载、网络和进程权限。本文只覆盖锁定的 DefaultAgent 与 LocalEnvironment 路径，不评价其他环境或部署。迁移价值在于边界清楚，而不是主张实现越小越好。

## 最容易误判的地方

第一类风险是把 LocalEnvironment 当作沙箱。它直接继承宿主进程能够访问的文件、命令和网络；即使调用前有模型提示词或命令格式约束，也不能替代操作系统隔离。需要隔离时，应显式选择 Docker 等不同环境实现，或在 Harness 外提供受限账号、容器和网络策略，并验证真实生效状态。

第二类风险是把框架退出当作产品成功。打印提交标记、触发 `exit` 或返回 submission 都不能证明测试通过、需求满足、没有破坏性副作用。第三类风险是把格式错误重试成通过：基础设施恢复可以再次尝试，但真实任务失败不能通过反复运行后只保留一次成功来改写。

## 怎样亲手核对

建立三个最小实验。正常链让模型返回一个只读动作，确认观察消息进入下一轮；限额链把 step limit 设为一，确认第二次查询前形成 `LimitsExceeded`；边界链在临时目录运行 LocalEnvironment，记录它确实使用宿主进程与环境。所有实验都只证明锁定版本在受控输入下的行为，不外推到其他环境、模型或部署。

核对时同时检查两组信号：控制流信号包括退出状态、调用次数与异常；任务信号包括预期文件差异、测试、禁止副作用和评分结果。只有后一组满足冻结任务契约时，任务结果才算通过。控制流日志用于解释结果，不能替代结果本身。

## 读完后做四个判断

### 问题 1

最小实现是否意味着没有恢复？

**答案：** 不是。源码包含格式错误恢复、限额和轨迹保存，只是恢复策略较集中，必须逐分支核对。

### 问题 2

LocalEnvironment 是否默认安全？

**答案：** 不能这样判断。它明确直接执行宿主命令，安全边界取决于外部进程权限和所选环境。

### 问题 3

出现 Submitted 是否可以计为通过？

**答案：** 不可以。它只表示 Harness 收到提交协议，仍需独立检查产物和任务契约。

### 问题 4

为什么它只是扩展样本？

**答案：** 它突出最小循环的教学价值，但不承担一级主线需要的完整产品表面、部署与治理覆盖。
