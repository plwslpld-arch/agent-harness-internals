# 贡献指南

## 欢迎什么

- 修正与锁定 commit 不一致的路径、行号、符号或行为描述；
- 补充跨包的因果链、失效条件、以及和其它 harness 的机制对照；
- 提交真跑过的实验记录（环境、命令、退出码、产物）；
- 改进校验脚本。

## 不接受什么

- 上游文档的翻译或复述——上游自己有 110 篇英文文档、683 篇设计记录；
- 纯 API 手册、入门教程；
- 没跑过却写成「已验证」的实验；
- 伪代码冒充源码。

## 写作规则

写作规范在 [AGENTS.md](AGENTS.md)，那份文件同时约束人和 AI 协作者。核心两条：**先给读者看模型真实看到的东西**；**引用源码要贴真代码 + `路径:行号`**。

## 本地跑一遍

```bash
npm run bootstrap   # 按 lock 拉取 5 个上游 checkout
npm run check       # 全部门禁
```

`npm run check` 里 `check:anchors` 会抽查正文中每一处 `文件:行号` 引用是否指向真实存在的行——行号写错、上游漂移都会在这里失败。这是本仓库对「结论可追溯」的唯一硬保证，请不要绕过它。

## 提交流程

1. 从 `main` 建分支；
2. 不提交 `sources/checkouts/`、依赖、构建产物、模型权重、真实日志或密钥；
3. `npm run check` 通过；
4. PR 里写清楚：改了什么、依据的上游 SHA、跑过什么。

## 第三方内容

上游源码、论文、商标不因本仓库改变许可。边界见 [THIRD_PARTY.md](THIRD_PARTY.md) 与 [sources/sources.yml](sources/sources.yml)。无法确认再发布权利时，只放链接、SHA、少量合规引用和自己的分析。
