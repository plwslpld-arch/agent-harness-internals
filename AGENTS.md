# Agent Harness Internals 仓库规则

本仓库是一套面向开发者的中文 Agent Harness 源码导读。六条主要课程是 DeepSeek Harness、Codex、Gemini CLI、Claude、pi 和 OpenCode；其他项目只在能补充独特机制时作为案例出现。Agent Harness 是内容主线，Eval 只在运行结果、轨迹和验证机制需要时进入正文。

## 写给谁

默认读者会一种编程语言，了解 Git、命令行和基本 API，但没有读过 Agent Harness 源码。文章不能假设读者同时熟悉 TypeScript、Rust 和 Python。遇到语言特有语法时，用中文解释它在调用链中的作用，不把语法细节当作理解前提。

## 公共内容

- README、正文、图中文字、图例、替代文本、About 和 Social Preview 使用中文。
- 产品名、协议名、API、函数、类型、字段、枚举和代码标识符保留原文。
- 普通文案统一写「DeepSeek Harness」；`$DSH_HOME`、包名、配置键和源码路径等真实标识符保持原样。
- 不创建平行英文 README。文件名使用稳定的 ASCII slug，文章标题和说明使用中文。
- 用户可见文字不出现本机绝对路径；命令从 checkout 根目录运行。
- 公共文章不展示内部进度、阶段记录、发布状态、内部结论编号或复核流程。

## 内容架构

`README.md` 是简洁入口；`docs/00-start-here.md` 提供学习地图；`docs/foundations/` 只讲读源码所需的最低共同知识；`docs/harnesses/` 是六条主要源码课程；`docs/comparisons/` 在课程之后回答同一问题；`docs/samples/` 收录单机制案例；`docs/labs/` 提供可复核实践；`sources/` 锁定上游提交和许可证；`scripts/` 维护机器检查。

不为旧文章数量、编号和模板保留兼容入口。文章边界由读者问题和真实源码结构决定。

## 源码导读

源码文章从一个真实任务或公开契约开始，再沿调用链阅读。每个关键源码站点应说明：

1. 为什么来到这个文件或符号；
2. 谁调用它；
3. 输入从哪里来；
4. 读取或修改了什么状态；
5. 返回给哪个模块；
6. 下一步读哪里；
7. 哪个上游测试、固定 Trace 或公开行为可以核对。

源码引用使用锁定提交的 GitHub 永久链接，并保留正确的语言高亮。上游真实源码、基于调用关系的机制解释、教学伪代码必须分开标注。行号只是定位工具，正文仍要解释数据怎样流动。

Claude 课程严格区分 Claude Code 闭源产品、官方公开契约、Python Agent SDK 源码和 TypeScript Agent SDK 可见内容。公开 SDK 不能证明闭源产品的内部实现；看不见的部分直接说明无法由当前来源核对。

## 图示

图只用于文字难以表达的结构、调用、状态或时序关系。每条主要课程至少有一张系统地图和一张端到端任务图。图中主要节点尽量控制在八个以内，必须有中文标题、中文说明和中文替代文本。函数名、类型名与协议名可以保留原文。

## 实践

实践分成三类：无需运行环境的源码核对、无需模型账号的确定性本地核对、需要凭据的可选真实模型核对。真实调用必须说明成本和不确定性，核心学习路线不能要求读者同时拥有六家模型账号。

## 来源与验证

所有来源绑定完整 Commit，Manifest、Lock、Checkout 和许可证保持一致。上游源码只做本地核对，不复制到公共正文。修改脚本行为时先写失败测试；修改 Markdown 后运行内容、链接、图示、来源和文风检查。完整验证使用 Node 24，直接调用已配置运行时，不调用 NVM。

机器检查负责发现可客观判断的问题，例如失效链接、错误来源、绝对路径、错误命名和缺失的源码链接；它不能代替人工判断文章是否讲清楚。
