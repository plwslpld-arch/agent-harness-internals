# 安全政策

## 报告安全问题

请不要在公开 Issue 中提交真实 API Key、访问令牌、未脱敏会话、用户数据或
可直接利用的零日漏洞。使用 GitHub 仓库的 Security Advisories 私下报告。

## 仓库安全边界

- `DEEPSEEK_API_KEY` 和其他凭据只从环境变量或本机凭据存储读取。
- 公共 CI 不向来自 fork 的代码暴露 secrets。
- 上游更新提醒只生成只读报告，不自动改写课程或推送分支。
- 第三方来源锁定到完整 Commit SHA，更新时检查许可证和来源 URL。
- 公共仓库不包含第三方 `.git`、`node_modules`、模型权重、私人运行日志或受限源码。
- 自动生成内容不可覆盖人工分析；安全和架构结论需要人工复核。

## 不属于漏洞的内容

单纯发现某个插件「理论上拥有宿主进程权限」属于架构风险研究；若能证明其
绕过已有批准、沙箱或凭据边界，请按安全漏洞报告。
