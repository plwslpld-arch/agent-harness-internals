# Version tracking

Scheduled automation writes machine summaries here through reviewable pull requests.
Each snapshot records old/new commits, file and API changes, license changes, affected
documents, verification state and unresolved review questions.

The authoritative accepted baseline remains `sources/sources.lock.yml`. Seeing a newer
upstream commit does not automatically promote that commit into the knowledge base.
