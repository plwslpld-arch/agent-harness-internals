# 阶段 6：全仓审计、历史清理与 GitHub 部署计划

**目标：** 在不改变最终文件树、不改写人类作者身份和提交顺序的前提下完成全仓终审，受控移除历史提交消息中的 Codex/Claude 协作者尾注与 Claude Session 元数据，并把默认分支部署为公开的 `agent-harness-internals`。

**完成顺序：** 先完成内容与门禁最终提交，再快进主分支；之后建立仅本地备份引用并重写提交消息；核对树、作者、主题和拓扑不变后强制推送；最后应用 GitHub 元数据、上传社交预览、恢复保护规则并验证公开页面。

## Task 1：最终公开树与品牌审计

- [x] 检索旧 A/E 空壳、旧导航、英文入口、旧 Logo、绝对路径、旧仓库标识和双 Harness 定位；阻断项必须删除或修正。
- [x] 复核 README、总入口、81 张正式图、119 条 Claim、17 个来源、第三方许可证与敏感信息。
- [x] 使用 Node 24 运行 `check-all` 和 `verify-sources --profile all`，不调用 NVM。

## Task 2：历史元数据清理器

- [x] 测试先行：只删除指向 Codex 或 Claude 的 `Co-Authored-By` 尾注与 `Claude-Session` 行，保留人类尾注、主题、正文和中文。
- [x] 实现审计、试运行和显式 `--apply`；默认拒绝脏工作树、非目标分支和缺少本地备份引用。
- [x] 重写后强制核对提交数量、提交主题序列、人类作者序列、父节点数量序列和最终 Tree 完全不变。

## Task 3：最终内容提交与主分支合并

- [x] 记录重写前远端主分支 SHA、分支保护与公开元数据快照。
- [x] 完成阶段 6 内容提交并在隔离分支重跑全量门禁。
- [x] 主工作树干净时以 `--ff-only` 快进 `main`，不产生额外合并提交。

## Task 4：受控历史审计与保护分支部署

- [x] 核对本地带时间标识的备份引用存在；没有把任何本地备份分支推到 GitHub。
- [x] 审计 `main` 的二百二十三个提交，确认消息级重写已经完成，Tree、人类作者、提交主题、顺序和拓扑保持不变。
- [x] 确认历史中不存在目标尾注或 Session 行；远端是本地主线祖先，因此遵守保护规则改用受保护 PR 与 rebase 合并，不执行不必要的强制推送。

## Task 5：GitHub 元数据与社交预览

- [x] 把仓库改名为 `agent-harness-internals`，更新 Origin、中文 About、Topics、Issues、Wiki、可见性和默认分支。
- [ ] 上传 `assets/brand/social-preview.png`，在 GitHub 设置页和公开仓库页验证预览、Logo、README 与相对链接。
- [x] 应用 main 分支保护：要求 `verify`、管理员同样受约束、禁止强推和删除。

## Task 6：公开状态验证与最终复核

- [x] 通过 GitHub API 和公开网页核对仓库名、About、Topics、默认分支、License、保护规则、最新提交与工作流。
- [x] 核对 Contributors 页面不再出现 Codex 或 Claude；API 只返回人类账号和匿名人类提交者，公开页面无对应贡献者条目。
- [ ] 写入阶段 6 对抗复核，勾选总路线，提交并普通推送最终记录。

## 完成定义

1. 本地与 GitHub 默认分支指向同一最终提交，工作树干净。
2. Node 24 的 17 组聚合门禁、17 个全部来源和所有高优先级复核项通过。
3. 历史清理只改变提交消息及其派生 SHA；最终 Tree、提交数量、主题、人类作者和拓扑保持不变。
4. GitHub 名称、About、Topics、默认分支、保护规则和 Social preview 与仓库机器元数据一致。
5. 公开 Contributors 不含 Codex 或 Claude，完成声明附带本地命令与公开状态证据。
